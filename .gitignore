# Dependencies — recreated by `pnpm install`, never committed
node_modules/

# Build output — generated from source, never committed
dist/

# Secrets (database passwords, API keys)
.env
.env.*

# Editor + OS files
.idea/
.claude/
*.log